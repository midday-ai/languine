# iOS

Languine obsługuje pliki Localizable.strings dla iOS, standardowy format lokalizacji dla aplikacji iOS. Ten format obsługuje podstawowe pary klucz-wartość i wspiera komentarze, które dostarczają kontekstu dla tłumaczy.

---

## Konfiguracja

Najpierw upewnij się, że masz plik konfiguracyjny languine.config.json lub languine.config.ts w katalogu głównym projektu. Oto przykład:

```json
{
  "locale": {
    "source": "en",
    "targets": ["sv", "de", "fr"]
  },
  "files": {
    "ios": {
      "include": ["[locale].lproj/Localizable.strings"]
    }
  }
}
```

## Tłumaczenie

Gdy Twoja konfiguracja jest ustawiona, uruchom:

```bash
npx languine@latest translate
```

Kiedy uruchomisz to polecenie, Languine:

- Załaduje Twój źródłowy plik Localizable.strings (np. en.lproj/Localizable.strings)
- Wykryje wszelkie nowe lub zmodyfikowane ciągi tłumaczeń
- Wygeneruje tłumaczenia dla Twoich docelowych języków
- Utworzy lub zaktualizuje pliki językowe docelowe (np. sv.lproj/Localizable.strings)
- Zachowa wszelkie istniejące komentarze i formatowanie
