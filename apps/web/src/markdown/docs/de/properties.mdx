# Eigenschaften

Languine unterstützt Java Properties-Dateien, das standardmäßige Lokalisierungsformat für Java-Anwendungen. Dieses Format verarbeitet Unicode-Escaping und unterstützt sowohl Schlüssel-Wert-Paare als auch Kommentare.

---

## Einrichtung

Zuerst stellen Sie sicher, dass Sie eine languine.json-Konfigurationsdatei im Stammverzeichnis Ihres Projekts haben. Hier ist ein Beispiel:

```json
{
  "locale": {
    "source": "en",
    "targets": ["sv", "de", "fr"]
  },
  "files": {
    "properties": {
      "include": ["src/main/resources/messages_[locale].properties"]
    }
  }
}
```

## Übersetzung

Mit Ihrer Konfiguration führen Sie aus:

```bash
npx languine@latest translate
```

Wenn Sie diesen Befehl ausführen, wird Languine:

- Ihre Quell-Properties-Datei laden (z. B. messages_en.properties)
- Neue oder geänderte Übersetzungsstrings erkennen
- Übersetzungen für Ihre Zielsprache generieren
- Die Properties-Dateien der Zielsprache erstellen oder aktualisieren (z. B. messages_sv.properties)
- Unicode-Escaping automatisch behandeln
- Kommentare und Formatierung beibehalten
